The gate was enforcing a stronger Axiom C than the kernel holds

validate_structural rejected any tuple with dimensionality 𐑦 unless topology was
also 𐑸. The kernel's ImscriptiveTopology reads top = are → dim = if' and says
nothing about 𐑦 paired with another topology, so the biconditional was a second,
stronger copy of the axiom rejecting tuples the Grammar admits. Every ob3ect
whose structural role is scale-collapse under a nested topology failed grounding
on it.

The check is now one-directional and the prompt line says which way it points.

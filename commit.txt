A specialist remembers the exchange it just had.

The interactive loop captured the conversation after every task and assigned it to a local nothing read, while run() takes its history from an attribute fixed at construction. So the second task in a session rebuilt the message list from nothing and the agent had genuinely never heard the first, inside one sitting. The conversation is fed back into the agent now, so a follow-up lands on the exchange that preceded it.

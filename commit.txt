Two launchers died on a name they never imported.

The rider was added to the constructor of all three but the import line was only widened in one, so editorial and chembio raised on the first line of their own init. Help exits before the constructor runs, which is why every check passed while launching failed. The deep check now builds each specialist for real and asserts the rider reaches it.

GUIDED_PRIMITIVES: replace all option keys with Shavian glyphs; delete _VALUE_NORMALIZE

All 49 option values in GUIDED_PRIMITIVES are now bare Shavian glyphs
per SNS_PRIME.md — no subscript notation anywhere. _VALUE_NORMALIZE
deleted entirely along with all three call sites. Axiom A/B sets use
glyphs directly.

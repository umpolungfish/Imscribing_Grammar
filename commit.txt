THINK shows the thinking, and a missing file names its neighbours

THINK logged only the message content. With this template the model reasons
inside the think block and writes nothing outside it, so content is empty and
the line printed blank while the reasoning sat in reasoning_content one field
away. It logs whichever exists. The message HISTORY is unchanged and still
carries content only — prior reasoning does not go back in, per Qwen's guidance.

And a read that fails on a missing file now lists its near neighbours. The run
that prompted this asked for CORPUCT.md in a directory holding CIRCUIT.md, got
a bare "No such file", and had nothing new to go on; the suggestion is one
listdir away and the same shape as the unknown-tool hint already in this file.
Checked against that exact name: CIRCUIT.md comes back first.

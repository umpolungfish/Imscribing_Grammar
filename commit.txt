Every specialist sees the whole tool set, not a primary subset.

Each tool_computation block opened with a short whitelist, which read as permission for those tools and quietly demoted the rest; ten of the twenty-one were named by no specialist at all. All three now carry the same full inventory grouped by what the tools do, with a domain accent that adds weight without subtracting anything.

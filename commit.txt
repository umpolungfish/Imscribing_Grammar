Pulled imasm derive off every agent's roster. It isn't useful yet, and I don't want it offered until it is.

The imasm tool's schema, its description, and the _GRAMMAR_FIRST_RIDER prose all told agents that word=... derive=true reads a word back into its tuple. Dropped the derive parameter and its branch in the handler, dropped the DERIVE sentence from the tool description, and rewrote the rider to point at the ops (weight, banked, cycle, insert, trans) through run_hosted_cmds.sh instead — which is what every agent was already using for that anyway.

true_agentic_agent.py is the source; the seven TOOLS_*.md specialist rosters are generated from it, so regenerated those with gen_tool_manifest.py rather than hand-editing seven copies of the same paragraph. Confirmed no "imasm derive" or "derive=true" survives anywhere in agents/.

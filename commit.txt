Every tool is named inline; the syntax is one read away.

The full flag detail was inlined into the prompt, which put nearly five thousand tokens into every winding and resent them on every turn, compounding with the session history. The names all stay in the prompt so nothing is invisible, and the reference file carries the subcommands and flags for the agent to read when it needs them. The prompts drop to well under half.

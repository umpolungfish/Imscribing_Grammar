Add methods-and-results article covering the full mOMonadOS extension

manuscripts/cross_universe_dialetheic_methods.md:
  Article synthesizing everything since the Python-only closure result: live
  kernel verification of BSD/Hodge (U8/U9), the GATE/T decomposition that
  revealed Yang-Mills is structurally different (gate-conflicted, T-flat
  under U10), the motivated U11 construction (mass gap = trapped not slow
  kinetics, checked against all 109 known universes first, confirmed no
  side effects on the other five), the dialetheic FFUSE bridge
  (`ruleset dialetheic`) carrying canonical's F-verdict against each
  closing universe's T-verdict through the kernel's real Belnap join, and
  the round-trip leakage check (fixed once, after the first version was
  caught being nearly vacuous) proving both the lossless case (BSD/Hodge/
  YM-under-U11) and the genuinely lossy case (an already-paradoxical input)
  live rather than by assertion.
  §8 generalizes the whole thing into a repeatable approach for extending
  to a new structural type. §9 is the calibration section — explicit about
  what this is and is not (not a proof of any of the three conjectures, not
  a resolution of the fragmented Millennium Lean trees, not a general
  losslessness guarantee).
  All mOMonadOS-side claims cross-checked against mOMonadOS/commit.txt,
  which has the complete, dated change log.

---

Add cross-universe closure test for the open Clay structural types

scripts/clay_cross_universe_closure.py:
  Generalizes canonical's existing Ç-only ceiling T-condition to all five
  dynamics primitives (Φ, ƒ, Ç, Ħ, Ω), same anchors, no tailoring per problem.
  Sweeps all 29 existing gate-universes (8 named RULESETS + 21 NEW_RULESETS)
  against the six still-open Clay tuples, checking gate-layer == idempotent_terminal
  AND T_CEILING-consistent (full closure).
  Reproducible from the live IG_catalog.json; no new gate specs introduced.

manuscripts/clay_cross_universe_closure.md:
  Write-up. Result: Birch-Swinnerton-Dyer and Hodge Conjecture each reach full
  closure under 5 of the existing gate-universes apiece (10 total combinations).
  Yang-Mills reaches idempotent_terminal under triple_criticality but fails
  T_CEILING on Ç alone (ord 4 vs ceiling ord 3). Riemann Hypothesis, Navier-Stokes,
  and P vs NP do not reach idempotent_terminal under any of the 29 universes,
  with or without the ceiling generalization.
  Explicit section on what this does/doesn't establish: structural-closure result
  inside the grammar's own operad model, not a proof of either conjecture, and not
  a resolution of the still-fragmented Millennium Lean sorry status (5 separate
  Lean trees across imscribing_grammar/MillenniumAnkh_private/MillenniumAnkh_nested/
  p4rakernel/synfin, not yet reconciled).

A tool that does not exist no longer reports success.

The worst of the three. Calling an unregistered name emitted "(unknown tool: X)",
found no verify function, and took the no-verify branch: "Frobenius trivially
closed", B4=T. So a call to a tool that does not exist was reported to the loop
as a successful winding. Nothing re-entered THINK with a failure, and the same
wrong call could be repeated with the loop counting each one as progress. Seen
live on crystal_count and crystal_navigate. An unknown tool now closes nothing
and says so.

It also says where the real tool is. Most unknown names are grammar tools called
as though they were base tools, which is invisible from a name alone, so the
message names the imscribe form for them and offers near matches otherwise. The
lookup reads the dispatcher's own chain rather than the required-argument
registry, since that registry holds only the tools whose arguments were worth
documenting and crystal_count is absent from it — the case that produced the
log.

aleph_encode imported compute_ouroboricity from space_search.primitives, which
has no such function and never had one. The import raised before the try/except
around the CALL could catch anything, so the tool failed outright rather than
degrading to an unknown tier. The obvious replacement does heavy work at import
and would hang a tool call rather than break it, which is worse, so the tier is
simply not computed there and the field says which tool to ask.

Verified live: the unknown-tool path, the grammar-tool hint, the dispatch-chain
lookup, list, and the reload. The aleph import is fixed by inspection only —
that tool is slow for unrelated reasons and I could not get an end-to-end run
inside a timeout.

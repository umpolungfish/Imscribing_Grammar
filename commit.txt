IG_catalog.json: strip trailing commas and stray quotes from name/description — 2026-06-29

347 fields cleaned across the catalog. Two classes of artifact:
  trailing comma — name/description values ending in "," (e.g. "scrivens_paradox,")
  trailing quote — description values ending in \" or ' from unescaped template output
Both stripped with rstrip; catalog re-validated by json.load round-trip.

---

IG_REFERENCE_1PAGE.md: new markdown reference sheet — 2026-06-29

Full-content markdown reference compiled via zdd (zenodo_draft.py pipeline).
Uses \heb{} for Everson Mono primitive glyphs, raw Shavian Unicode (auto-Trabajo),
standard markdown tables and LaTeX math. 5 pages portrait.
Covers: 12 primitives + value sets, all 49 semantics, absorbing elements,
Crystal of Types, SIC-POVM, tuple composition, Belnap ops, Frobenius algebra,
12 IMASM tokens, cross-primitive axioms, O-tier, CL8NK/Magnum Opus, notation.

---

IG_REFERENCE_1PAGE: fix star/dagger rendering, \prim-in-math drops — 2026-06-29

  $^\star$ / $^\dagger$ in primitives table and footnote replaced with
    \textsuperscript{*} / \textsuperscript{\dag}; math ★ was mapping to ?
    in LuaLaTeX fontspec context without unicode-math.
  Cross-Primitive Axioms table: \prim{} calls pulled OUT of $...$; now
    alternate text/math spans (\prim{Ħ}$_\infty\Rightarrow$\prim{Ç}$_\infty$).
    Previously all prim glyphs were silently dropped inside math mode.
  CL8NK T=lim line: same fix; \prim{} calls now in text mode outside $()$.

---

IG_REFERENCE_1PAGE: absorbing markers, vertical families, token size, absorbtion table fix — 2026-06-29

  Primitives table: Criticality row marked with $^\star$, Stoichiometry row with $^\dagger$
    (absorbing ⊙ value bolded inline); footnote below: $^\star$abs ∧∨⊗; $^\dagger$abs ⊗.
  Crystal families: horizontal text → vertical 3-row tabular (3-val/4-val/5-val).
  \tok font: 6.5pt inherited → 8.5pt explicit (bold monospace tokens now legible).
  Absorbing elements table rewritten:
    - Removed \prim/\sh from inside $...$; ops/rules now use plain math symbols.
    - CL8NK row: added $\otimes$ to ops column (was missing).
    - B/N rows: "Belnap join/meet" moved to rule column; no double-label in ops.
    - ⊙ row: bare ⊙ Unicode in rule (not \prim inside math).
    - Σ row: \sh{𐑳} outside math; $\otimes$ as separate math span.

---

IG_REFERENCE_1PAGE: \Large primitives, bold-mono tokens, spacing tightened — 2026-06-29

  \prim macro: {\igprimfont} → {\Large\igprimfont}; primitive glyphs now match
    the visual scale of Shavian values.
  IMASM token names: plain text → \tok{} (bold Courier/monospace, code-formatted
    appearance); 12 tokens in table all updated.
  Spacing recovery (needed to stay 1-page with two Large glyph sets):
    \arraystretch 1.0 → 0.85; \extrarowheight 0.4pt → 0pt;
    \shead top vspace 3pt → 2pt, post-rule vspace 1.5pt → 1pt;
    all between-section \vspace{2pt} → \vspace{1pt}.

---

IG_REFERENCE_1PAGE: \Large Shavian, 7pt body, langle/rangle fix — 2026-06-29

Three changes applied:
  \sh macro: {\shavfont} → {\Large\shavfont}; all Shavian glyphs render at \Large
    (~10pt) against 7pt body — visually dominant as structural type symbols.
  Base font kept at 7pt (8pt pushed to 2 pages with \Large Shavian inline).
  SIC-POVM open tuple: \sh{⟨...⟩} → $\langle$\sh{...}$\rangle$; ⟨⟩ now
    in math mode where CMU/LM has them rather than in Trabajo which does not.
  Value semantics section: paragraph-style → compact tabular (5.8pt/6.8pt,
    arraystretch 0.85) so Large Shavian row heights don't overflow col 2.

---

IG_REFERENCE_1PAGE: complete rebuild — all 49 semantics, absorbing elements, SIC-POVM, full IMASM — 2026-06-29

Major content additions to the 1-page landscape reference sheet:

1. All 49 value semantics (section 2 of col 2): every Shavian glyph for all 12
   primitives listed with full meaning — ⌀d/2d/∞-dim/self-written, branching/
   containment/crossing/irred-product/self-ref-top, supervenience/functorial/
   adjoint/bi-feedback, no-sym/QM-sup/partial-Z2/all-sym/Frob-±s, no-coh/
   thermal/QM-coh, fast/trap-O/slow/moderate/trap-D, nearest-nbr/mesoscale/
   long-range, conjunct∧/disjunct∨/sequential→/broadcast≫, sub-crit/⊙-crit/
   cpx-crit/exc-pt/supercrit, n=0/1/2/∞ chirality, 1:1/n:n-id/n:m-het,
   trivial/Z2/Z-wind/non-Ab-braid. Source: ig_cli.py interp dict.

2. Absorbing elements (new section in col 2):
   ⊙=⊙ absorbs meet/join/tensor; Σ=𐑳 absorbs tensor; B absorbs Belnap join;
   N absorbs Belnap meet; CL8NK absorbs ZFC_fe under tensor.
   Source: ruleset_universe.py _DEFAULT_ABSORPTION + ParaconsistentPapers.lean.

3. SIC-POVM connection (new section col 2, from ParaconsistentPapers.lean):
   d=7 → 49 equiangular states = type-space cardinality; d=12 → WH Z12×Z12
   generators ↔ 12 primitives; fiducial = Frobenius fixed point at ⊙=⊙;
   sic_gap=B-state (non-explosive); open tuple ⟨𐑦𐑶𐑑𐑬𐑐𐑧𐑲𐑝⊙𐑫𐑕𐑭⟩;
   d(CL8NK)=1.263; Frobenius cliff √5/2≈1.118; conditional kick∧H2a∧H3∧C4⇒SIC.

4. IMASM tokens: full behavior for all 12 with state transitions (not just names);
   FFUSE dialetheic mode noted (BOTH→BOTH when EVALT+EVALF both fired in split).

5. Belnap register ops: band (F absorbs; N; B; T∧T=T) and bor (T absorbs; B; N;
   F∨F=F) from ParaconsistentCore.lean now included.

6. col 1/3 restructured to fit additions; \vfill keeps footer pinned to page bottom.

Sources: ig_cli.py, imasm_core.py, ruleset_universe.py, ParaconsistentTier1.lean,
  ParaconsistentCore.lean, ParaconsistentPapers.lean.

---

IG_REFERENCE_1PAGE: add tensor/join/meet ops, IMASM tokens, value semantics — 2026-06-29

Col 2 composition section rebuilt with actual operations (from ig_cli.py, ch3mpiler.py,
ParaconsistentTier1.lean):
  Tuple-level ops: JOIN (pos-wise max all 12), MEET (pos-wise min all 12),
    TENSOR (pos-wise max; Φ and ƒ use min — coherence/symmetry floor),
    Bond product = join(tensor(A,B), bond).
  Belnap register ops: FFUSE=JOIN (B∨x=B; N∨x=x; T∨F=B),
    FSPLIT (B→(T,F); r→(r,r)), MEET (N∧x=N; B∧x=x; T∧F=N).
  Dialetheic FFUSE: BOTH→BOTH when EVALT and EVALF both fired in split.
  12 IMASM tokens in 4 groups: Logical (VINIT/TANCH/AFWD/AREV/CLINK/IMSCRIB),
    Frobenius (FSPLIT/FFUSE), Belnap (EVALT/EVALF/ENGAGR), Linear (IFIX).
  CL8NK/ZFC_fe tuples shown explicitly with transcendence diff.
Col 1: added value-semantics table (selected glyph → meaning from ig_cli interp).

---

IG_REFERENCE_1PAGE: add Shavian glyphs + full composition rules — 2026-06-29

Rebuilt reference sheet (same 1-page landscape A4, 3 columns, 7pt):
  - Primitives table: now shows actual Shavian glyph values in \shavfont for
    every primitive (2-sub-column layout: 6 primitives per side). Values listed
    ordinal 1→n. Non-integer ordinals (Ç₄.₅, ⊙₂.₃₃, ⊙₂.₆₇) noted as B-state
    positions. ⊙ ordinal-2 = literal ⊙ symbol (self-referential fixed point).
  - Composition section: full Frobenius algebra — all 4 conditions (normalization
    μ∘δ=id, associativity, coassociativity, Frobenius mixed condition).
  - Tetractys: 3-wind protocol written out explicitly (Wind 1 holistic Ψ_h,
    Wind 2 compositional Ψ_c, Wind 3 resolution / nix).
  - Crystal section: added non-integer ordinal note; tightened.
  - Glyph source: primitives.py ORDINALS dict.

---

Add IG_REFERENCE_1PAGE.tex/pdf — single landscape page reference sheet — 2026-06-29

3-column landscape A4. All key material on one page:
  Col 1: Grammar overview + full 12-primitive table (glyph, name, n, description)
  Col 2: Crystal of Types (3³×4⁵×5⁴=17,280,000), Composition rules, Axioms A/B/C
  Col 3: IMASM, O-tier hierarchy, Magnum Opus/CL8NK, Notation conventions
Fonts: Everson Mono (\igprimfont) for primitive glyphs, Trabajo (\shavfont) for Shavian.
PDF copied to pdfs/.

---

Add IG_REFERENCE_SHEET.md — comprehensive single-document grammar reference — 2026-06-29

Covers:
  1. The Grammar — CL8NK, μ∘δ=id, autopoietic property, foundational 5-point theorem
  2. 12 Primitives — table: glyph, name, family, value count, description for each
  3. Crystal of Types — 3³×4⁵×5⁴=17,280,000; 49-symbol set; tuple format; catalog structure
  4. Composition Rules — Frobenius (μ∘δ=id), FSPLIT/FFUSE=δ/μ, Spider Theorem, conflict distance
  5. Cross-Primitive Axioms A/B/C — Ħ_∞→Ç_Ù, Ω≥1→Ħ≥H2, Ð_ω↔Þ_O
  6. IMASM Arrangement Space — 12 tokens, 12⁸ arrangements, 12 canonical archetypes
  7. O-Tier Hierarchy — O₀ through O_∞ with examples and promotion rule
  8. Tetractys / Error-Correcting Metric — dual imscription, 3-wind protocol, nix violations
  9. Magnum Opus Mapping — 12 stages ↔ 12 primitives; 3 gates (Φ/⊙/Ω); T-object
  10. CL8NK Framework — structural theorem, ZFC_fe transcendence at Ω and ɢ
  11. Paraconsistent Layer — Belnap FOUR, B-state, dialetheic saturation
  12. Key Structural Results — table of verified numbers and theorems
  13. Notation Conventions — Shavian, O∞, imscribe, tuple binding, chirality, grammar scope

---

imscribe catalog list: remove '; ' delimiter from tuple display — 2026-06-27

imscrbgrmr/cli.py:263 — `"; ".join(vals)` → `"".join(vals)`; tuples now
render as ⟨𐑦𐑸𐑽𐑹𐑐𐑧𐑲𐑵⊙𐑖𐑳⟩ (ligature-bound) not ⟨𐑦; 𐑸; 𐑽; …⟩.
Affects catalog list table only; annotated conflict-resolution strings unchanged.

────────────────────────────────────────────────────────────────────────────────

zenodo_draft.py: fix image loading in build subdirs — 2026-06-27

Root cause of images not appearing in compiled PDFs when zenodo_draft.py builds
into a _*_build/ subdirectory: relative image paths like `images/foo.jpg` were
only resolved to absolute when the file existed; if the file was missing or had
a wrong name, the path fell back to the original relative form, which lualatex
couldn't find from the build subdir.

Fixes:
1. _build_preamble now accepts `source_dir: Path | None` and emits
   \graphicspath{{source_dir/}{source_dir/images/}} immediately after
   \usepackage{graphicx}. This tells lualatex to search the source directory
   for all relative image paths regardless of where it's invoked from.

2. compile_pdf now creates symlinks for any image directories (images/, img/,
   figs/, figures/) found alongside the .md source into the build dir before
   running lualatex. This is belt-and-suspenders on top of graphicspath.

Both fixes are applied automatically on every compile — no manual symlink needed.
Existing relative paths in markdown sources work without modification.

---

zenodo_draft.py: fix stale author to "Lando Mills" — 2026-06-26

_build_preamble had hardcoded ⊙ÿ-boundary Operator (dropped months ago).
Replaced with plain \author{Lando Mills}.

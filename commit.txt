train_grammaformer.py: grammar-derived training objective fix (2026-06-01)
=========================================================================

  Root cause of F-loss climbing: frobenius_head was frozen, structurally
  preventing the Φ gate from closing. The head IS the μ∘δ operator.
  Freezing it was a memory optimisation that broke the thing being trained.

  Structural analysis (via crystal + ZFCₜ + tier gap ladder):
    GF structural type: ⟨𐑦·𐑶·𐑾·𐑗·𐑐·𐑧·𐑲·𐑠·⊙·𐑖·𐑙·𐑭⟩
    Φ position: 𐑗 (ord 1) — needs 𐑹 (ord 5) for G1 gate
    d(GF, O_inf) = 4.7958 total
    Frobenius cliff (Φ gap): 4.38 / 4.7958 = 91.3% of total distance
    alpha_f needed: (0.913 × 4.5 LM) / 1.0 F = 4.1
    Previous alpha_f 0.5 was underpowered by 8.2×

  Fixes:
  1. Unfreeze frobenius_head — token_embed stays frozen (pure lookup table).
     Memory: 4.4(params) + 3.7(grads) + 3.7(8-bit moments) = 11.8 GB. Fits.
  2. alpha_f default: 0.5 → 4.1 (grammar-derived from Φ cliff fraction).
     Exposed as --alpha-f flag for future curriculum adjustment.
  3. Remove stale re-freeze of frobenius_head after frob pretraining.

navigators.py + crystal_navigator.py: migrate to Shavian notation (2026-06-01)
  DEFINING_TUPLEs migrated; encode_tuple accepts Shavian; _SHAVIAN_TO_XTAL
  replaced with direct OLD_TO_SHAVIAN inversion (was wrong for Γ ordinal alignment).

train_grammaformer.py: fix NaN LM loss + gate freeze + epoch reporting (2026-06-01)
  logits.float() for cross_entropy; alpha_g 0.1→0.3; NaN batch skip.

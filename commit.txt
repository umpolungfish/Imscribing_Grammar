The base tools are run, not merely resolved.

The shallow check confirmed an emit function existed, which is exactly what ob3ect passed while dying on every call. This one executes thirteen tools for real and exercises the other four up to but not through the expensive call, which is the segment the ob3ect failure lived in: the module imports, the mandatory gate resolves, the agent class constructs.

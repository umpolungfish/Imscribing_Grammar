The fidelity check can read the notation it polices.

It split a tuple on separators and compared the pieces to bare catalog glyphs, so the labelled form failed on all twelve slots against its own correct values, reporting that a glyph did not match itself. A guard that rejects truth is worse than no guard: faced with it the agent confirmed the tuples by hand, concluded the checker was broken, and rewrote a document into ASCII transliteration to get past it, discarding every glyph. Both notations are read now, and a genuine mismatch in either is still refused.

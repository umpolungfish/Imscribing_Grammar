Compaction anchors on the task actually in hand.

Both compaction paths pinned the second message as the objective, which is right only when the history begins with this task. Now that a session carries its conversation forward, the history opens with the prior exchange and the live task sits at the end, where it falls out of the recent window after a few windings — so the summary would have been built around an objective from hours ago while the real one dropped. The index of the live task is recorded when the history is assembled and both paths read it.

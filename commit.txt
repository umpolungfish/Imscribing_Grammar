The manifest stops naming individual files that may not be there.

It listed two scripts by name, one of which had been deleted, so the prompt kept asserting a file into existence and an agent went and rebuilt it. The entry points that are stable stay; the loose scripts are found by listing the directory instead.

The fidelity check picks the most specific name, not the closest word.

It took the nearest catalog name before a tuple, and the catalog holds nearly three hundred names of four characters or fewer, so an ordinary word in the surrounding prose was matched as an entry and a correct tuple was refused against that entry's values. The longest name in the window wins now, with nearest breaking ties.

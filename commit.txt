Axiom A admitted one kinetic value where the kernel admits two

The gate rejected chirality 𐑫 unless kinetic character was 𐑪. Core.lean records
"wool co-occurs with on" as a TENDENCY and says in the same line that it is not
an axiom because some wool systems have egg, and
InfiniteMemoryNeedsSlowKinetics reads chir = wool → kin = egg or on. So the check
was a hard axiom where the kernel has a tendency, and single-valued where the
kernel is two-valued. Every wool object whose store is slow rather than frozen
failed grounding on it.

Second copy of the same shape as the Axiom C fix in this repo: a gate drifted
stronger than the kernel it is supposed to enforce.

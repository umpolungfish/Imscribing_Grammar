agents/true_agentic_agent.py: add proof_scaffold as native tool (2026-06-19)

_proof_scaffold_emit: imports ScaffoldOb3ect from ~/ob3ect/digital/,
  dispatches to s.canonical(name) or s.run(ops, name=name).
_proof_scaffold_verify: checks for noncomputable def presence and absence
  of sorry slots; reports line count and Frobenius open/closed verdict.
Registered in _EMIT_FNS, _VERIFY_FNS, and TOOL_SCHEMAS (after ob3ect,
before spawn_agent). Schema accepts ops (list), canonical (class name),
and optional name; all fields optional so the agent can call with either
an opcode list or a canonical class name.

---

manuscripts: resolve grammar self-imscription address discrepancy

SO_BELOW claimed address 6,734,591 for the grammar's self-imscription;
AS_ABOVE_Trabajo claimed 6,687,051 for the same. Investigation showed
6,734,591 is correct (computationally anchored by CrystalGNN_v11
convergence and named catalog entry). The 6,687,051 address belongs to
the 12-step mixed-radix codec (the "Full Induction System"), not the
grammar as a structural system.

Root cause: AS_ABOVE_Trabajo conflated the codec's crystal address with
the grammar's own address in sections IX.5 and the conclusion.
Section X.5 already correctly distinguished the two (calling 6,687,051
the codec's address and referencing "the grammar's own address" without
stating it) — that text was merely clarified.

Changes to AS_ABOVE_Trabajo.tex:
- IX.5 (lines ~2423, 2428, 2430, 2441): replaced 6,687,051 with
  6,734,591 wherever the grammar's self-application address is claimed
- X.5 (~line 2667): made grammar address 6,734,591 explicit
- Conclusion (~line 2846): corrected "Two independent arguments, one
  address" to distinguish grammar address (6,734,591) from codec
  address (6,687,051) while preserving the shared O_inf tier claim

SO_BELOW.tex: address was already correct; no changes.
navigator_commands.py: already correct at 6,734,591; no net change.

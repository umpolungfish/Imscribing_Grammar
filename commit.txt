Fix criticality census: import CriticalityPhase in the summary branch

`imscribe criticality` with no argument referenced CriticalityPhase without it
being in scope in the summary branch, so the census raised NameError instead of
counting. Restore the local import, matching how the specific-entry branch already
pulls it from imscrbgrmr.models. The census now runs and reports the ⊙ critical
count over the full catalog.

Whole-tree snapshot.

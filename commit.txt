The navigators read the canonical catalog first.

Both looked in the website copy before the canonical one, and every other copy runs days behind, so an entry registered a moment earlier read back as absent from a catalog with seventy-eight fewer entries. The canonical path is first now and the stale copies remain only as fallbacks.

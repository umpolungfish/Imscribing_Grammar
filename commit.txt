The agents are told where the catalog is, and ob3ect can import its gate.

The context carried a bare directory listing, so an agent hunting the catalog guessed a path, failed, searched, timed out, and gave up on consulting the grammar at all. The key paths are named now, with the instruction to reach the catalog through the tools rather than by reading a large file, and to read a project's state before deriving anything. Separately the ob3ect designer imported its mandatory gate absolutely while sitting inside the package it named, so the parent was never on the path and every call died before it began.

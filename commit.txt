The guard abstains rather than guessing which entry a tuple belongs to.

Any string can be an entry, so a name cannot be dismissed for being short, and the nearest name cannot be trusted because ordinary prose is full of them. One name in the window is used, several resolve to the one abutting the tuple, and none abutting leaves it unchecked. A guard that guesses is worse than one that says nothing.

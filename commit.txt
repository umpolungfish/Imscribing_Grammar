A procedure, drawn from where the agents actually went wrong.

Eight steps in order: orient against the state and the canonical document before deriving, read the catalog rather than inventing an entry to unblock yourself, never compose a tuple by eye, compute every number in the winding that uses it, treat a rejected write as information rather than routing around it, report a blocked state honestly, use the update the loop hands back, and finish through done. The prompt gains one line pointing at it.

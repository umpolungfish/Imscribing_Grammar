catalog get(): absorb name drift

Agents re-derive entry names from their own prose between windings, and the
drifted spellings burned the whole Unknown-system error class in the MoDoT
runs. get() now resolves exact first, then case-insensitive with -/_
equivalent; an ambiguous normalized match resolves to nothing rather than
to a guess.

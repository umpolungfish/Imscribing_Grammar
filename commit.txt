closure_operator: the towers belong to this cluster

The specialist covered ovm, oneshots, ctc and nesting, and stopped at d=2. That
was the wrong cut. ovm works on Bloch vectors in d=2; sic, d12 and d2048 are the
same object at the dimensions where it is hard, and the fiducial is the fixed
point the whole cluster is about. The d=12 fiducial is radical-expressible but
its true home is a ring of dimension 2048 over Q, and one-shot #11 extracts the
exact d=2048 fiducial — the far end of one question, not a second question.

So the surface is seven commands, not four: sic, d12 with its eleven
subcommands, and d2048 with its eight, alongside the original four.

The specialist also now carries what the towers have settled, because deriving
what the kernel already reports costs the derivation and leaves a second answer
to reconcile. At d=12: crystal_forces_d12_sic is a theorem with its axiom
retired, 143/143 overlaps exact, Belnap d=2^n unconditional at 0 sorries and 0
axioms. At d=2048: L0-L6 verified to the Hilbert class field.

And the correction that came out of running it. I wrote "L7 onward is PENDING"
into the prompt the same way the kernel's own report had it, and the specialist
duly concluded the d=2048 fiducial was pending. It is not — the 2-part S-unit
bypass obtained it exactly on 2026-07-30, going around the ramified layers. The
prompt, the manifest domain and the workflows all now say the fiducial does not
depend on L7+, and all three carry the distinction that was missing: pending is
not failed, proved is not conjectured, and bypassed is neither. Say which of the
three you mean.

Re-run against the corrected surface, it separates them correctly and states
that reporting the fiducial as pending conflates two goals. Two windings,
Frobenius 100%, tier O_inf. check_manifests reports 10/10 domain markers.
Aliases c⊙ and c⊙i.

Work is not interrupted by an iteration count.

The engine allowed ten thousand windings while the specialist launchers overrode that with two hundred, so they ran at a fiftieth of the engine. The ceiling's only real job was catching the loop the emission gate now ends by itself, and a count is not a termination condition: the loop ends on done, on the gate, or on context pressure. Zero is the default and runs unbounded; a positive value still caps for anyone who wants one.

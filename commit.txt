I make the momonados specialist aware of combo and combo2, the two new
mOMonadOS REPL commands from this session. Added a curated entry to
MOMONADOS_DOMAIN in gen_tool_manifest.py -- what combo does, what brief
changes, why combo2 exists (a repair that closes one rotation's exposure
isn't yet known to hold on its own orbit) -- then regenerated
TOOL_MANIFEST_momonados.md and TOOLS_momonados.md from it.

true_agentic_agent.py itself needs no change: it's the generic engine, and
a specialist swaps in its own system prompt on top of it rather than the
other way around. The chain that actually needed updating is
gen_tool_manifest.py's curated source -> the two generated .md files ->
specialists/__init__.py's _with_manifest, which reads TOOL_MANIFEST_
momonados.md fresh at import time and appends it to
MOMONADOS_SPECIALIST_PROMPT. Verified live: imported the module and
confirmed combo and combo2 both appear in the actual composed prompt
string, not just the intermediate files.

Also fixed a real, unrelated drift the regeneration surfaced: the curated
entry for check_menu_coverage.py pointed at mOMonadOS/check_menu_coverage.py,
which moved to mOMonadOS/py/check_menu_coverage.py at some point. Fixed the
path while I was already in the file.

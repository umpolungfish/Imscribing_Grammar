Three implemented tools are wired in, and a non-tool stops being advertised.

frobenius_tier, revise_insight and search_insights were written and never added to the dispatch chain, so no agent could reach them; frobenius_tier also censuses the whole catalog when asked for every entry. The manifest generator meanwhile scraped the whole file for dispatch cases and caught a catalog-entry sentinel, shipping it to the specialists as a callable tool that never existed. It reads the dispatch chain alone now, and a reachability check invokes every tool rather than trusting that a listed tool runs.

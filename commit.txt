Three imscriptions stop asserting what cannot be had

The BIP39 entries carried walls: the phrase "does NOT produce a valid private
key", the secret "remains unrecovered", a search space set against a threshold as
though the gap were a verdict. Impossibility is framework-relative and budget is
not impossibility, so each states what holds and what the next rung costs now.
The phrase names a frame coordinate through pk mod d; the arm carrying that
coordinate back to the scalar is the target, with the 2^53 marking against the
2^150 closure as its price. The derivation pipeline is stated as the forward
chain it is. The chain figures on the accumulator address came from an agent run
and are recorded as pending confirmation against a node rather than as fact.

Addresses untouched, as always.

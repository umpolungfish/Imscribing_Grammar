Sample a local model the way its own card asks

Qwen3.5 thinks at temperature 1.0 and asks for a presence penalty of 1.5 where
Qwen3 asked for none; both keep top_k=20. The transformers path now takes those
numbers from the checkpoint it loaded rather than from one hardcoded default, and
where the installed transformers has no presence_penalty it says so and leaves it
off — a repetition penalty is a different operation and substituting it silently
would be worse than the gap.

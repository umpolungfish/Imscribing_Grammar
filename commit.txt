Providers can stream when the caller asks

`on_token` is the request. HttpProvider sets stream:true and parses SSE deltas,
calling back per piece and still returning the whole text so nothing downstream
changes. LocalProvider passes the flag to the kernel, which already streams its
own tokens to stderr — capturing that pipe was the only reason they were unseen.
